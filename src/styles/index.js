import { StyleSheet } from 'react-native'
import { layouts } from './_layouts'
import { fonts } from './_fonts'
import { boxes } from './_boxes'
import { buttons } from './_buttons'

export const layout = StyleSheet.create(layouts)
export const font = StyleSheet.create(fonts)
export const box = StyleSheet.create(boxes)
export const button = StyleSheet.create(buttons)
