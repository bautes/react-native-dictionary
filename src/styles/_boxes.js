import { WHITE, BLACK, RED } from './_palette.js'

export const boxes = {
  text: {
    height: '100%',
    width: '100%',
    borderColor: RED,
    borderWidth: 1,
    justifyContent: 'center',
    alignItems: 'center',
    alignSelf: 'auto',
  }
}
